# 开发模式：允许 mock / fallback / AUTO_CREATE_TABLES（仅本地开发兜底）
APP_ENV=dev
DATABASE_URL=sqlite:///data/app.db
AUTO_CREATE_TABLES=true

AGENT_PROVIDER=mock
AGENT_MODEL=mock-creative-writer-v1
AGENT_FALLBACK_TO_MOCK=true
